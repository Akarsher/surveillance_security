The directory `/Security_cam/known_faces/restricted` is intended to store images of restricted personnel for facial recognition. Since this is a directory meant for storing images and does not require any specific file contents, it will be left empty. 

Here is the response:

# This directory is intentionally left blank.